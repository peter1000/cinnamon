# Description: Javascript Bindings for Cinnamon.
# URL: http://cinnamon.linuxmint.com/
# Maintainer: https://github.com/linuxmint
# Packager: fanch at orange do fr thierryn1 at hispeed dot ch
# Depends on: js gnome-common cairo gobject-introspection js dbus-glib


name=cjs
version=2.6.2
release=1
source=(https://github.com/linuxmint/$name/archive/$version.tar.gz)

build() {
	cd $name-$version	
	./autogen.sh \
	--prefix=/usr \
	--disable-static \
	--libexecdir=/usr/lib
	make
	make DESTDIR=$PKG install
}
